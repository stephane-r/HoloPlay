import React from 'react';
import SearchbarAbsolute from '../../../components/Search/BarAbsolute';
import { memo } from 'react';

export const SearchbarAbsoluteContainer = memo(() => {
  return <SearchbarAbsolute />;
});
