services:
  - docker

before_script:
  - make docker-build
  - make setup

cache:
  directories:
    - node_modules
    - website/.screenshots
    - website/public/showcase/entry

script:
  - make yarn-install
